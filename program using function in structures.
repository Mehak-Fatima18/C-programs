#include <iostream>
#include<string>
using namespace std;

void display(char *s, int t) {
	cout << *s << "\t" << t;
}
int main() {
	struct book {
		char name[34];
		int pages;
	};
	struct book b1 = { "Cprg",500 };
	display(b1.name, b1.pages);
}
