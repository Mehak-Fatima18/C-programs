#include <iostream>
#include<string>
using namespace std;
template <typename T>
void display( T b1) {
	cout << b1.name<<"\t"<<b1.pages;
}
int main() {
	struct book {
		char name[34];
		int pages;
	};
	struct book b1 = { "Cprg",500 };
	display( b1);
}
